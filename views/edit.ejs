<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./styles/style.css">
</head>

<body>
    <div class="w-full h-[100vh] bg-zinc-900 p-10 text-white">
        <div class="nav text-blue-500 text-sm mb-10">
            <a href="/read">Read Users</a>
        </div>
        <h1 class="text-4xl mb-10 font-semibold">Update User</h1>

        <form action="/update/<%= user._id %>" method="post">
            <div class="old">
                <h1 class="text-xl mb-2 font-semibold">Previous Details</h1>
                <input type="text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
                    name="name" value="<%=user.name %>">
                <input type="text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
                    name="email" value="<%=user.email %>">
                <input type="text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
                    name="image" value="<%=user.image %>">
            </div>
            <div class="new">
                <h1 class="text-xl mt-5 mb-2 font-semibold">New Details</h1>
                <input type="text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
                    name="newname" placeholder="New Name">
                <input type="text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
                    name="newemail" placeholder="New Email">
                <input type=" text" class="px-5 py-2 border-2 border-zinc-800 outline-none rounded-lg bg-transparent"
                    name="newimage" placeholder="imageUrl">

            </div>
            <input type="submit" value="Update User" class="px-3 py-2 outline-none rounded-lg bg-blue-500 mt-5">

        </form>

    </div>
</body>

</html>